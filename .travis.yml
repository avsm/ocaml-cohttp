language: c
sudo: required
services:
  - docker
install:
  - docker pull avsm/docker-opam-build:debian-stable-ocaml-4.02.1-system
script:
  - pwd
  - docker run -v ~/build/$TRAVIS_REPO_SLUG:/repo avsm/docker-opam-build:debian-stable-ocaml-4.02.1-system sudo ls -la /repo
